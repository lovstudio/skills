# User Configuration (compatibility reference)

The old optional configuration reference is retained for links from earlier
Skill sources. New Skills always use the `user-profile/v1` contract described in
[`user-profile.md`](user-profile.md), regardless of whether `--user-config` is
present on the Creator command line.
