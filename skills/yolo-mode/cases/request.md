# 真实用户输入

日期：2026-09-07。来源：本次创建请求，保留原始措辞。

> 在有些时候我可能离开电脑（比如去吃饭或者睡觉），所以我希望在这种模式下，任务或 skill 的运行处于完全自动的状态：
>
> 1. 不要主动问用户问题：虽然以后也许可以通过一些人机交互手段解决，但目前希望它一切按照默认值、用户的预设，或者根据用户的一些 preference 来推断。
> 2. 绝对不要因为某些授权被卡住，然后傻傻地等待用户响应交互。
>
> 希望能有一个 skill，可以一键让 AI 处于完全自动值守的状态去干活

用户调用 lov-skill-creator 要求创建 Skill；本次验收对象是生成并安装的 Skill，
并非一次已经完成的整夜无人值守业务运行。

## 真实后续请求

> 改成 /lov-yolo-mode 吧，而且强制将当前 session 改成 yolo mode（否则更容易出现权限问题）以及enable 所有必要的权限
