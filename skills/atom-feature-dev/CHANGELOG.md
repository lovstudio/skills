# Changelog

## [0.2.2] - 2026-09-07

### Added

- 统一展示名为「功能工坊」，保持调用 ID 与能力契约。

## 0.2.1

- Make CSS and JavaScript asset URLs work from both HTTP and direct `file://` entry points.
- Add a clearly labelled read-only file preview backed by a bundled demo snapshot.
- Keep project reads, writes and surface execution exclusive to the loopback bridge.

## 0.2.0

- Add a runnable ADE-style Atom Workbench with Overview, Contract, Run, Compare, Review, Profile and Agent panes.
- Add a loopback-only Python bridge with explicit execution and Profile-write gates.
- Guard local HTTP requests by Host and support an actual IPv6 `::1` listener.
- Add manifest command arrays, Preset initialization, demo workspace and HTTP selftest coverage.
- Add headless Chrome desktop execution and 390px responsive verification; fix hidden loading-state rendering.

## 0.1.0

- Initial local Skill source.
