# Markdown to PDF demo

The bundled demo exists only to exercise the local Dashboard bridge and shared surface command contract.
It does not render a real PDF and is never presented as a production feature case.

