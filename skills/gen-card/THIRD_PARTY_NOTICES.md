# Third-party notices

The bundled `assets/modern-screenshot.js` browser runtime is from the
`modern-screenshot` project and is used under its MIT license. It is embedded so
generated HTML can export PNG offline without a hidden sibling-Skill dependency.
